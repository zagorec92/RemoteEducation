<Window x:Class="RemoteEducationApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:global="clr-namespace:RemoteEducationApplication.Properties"
        xmlns:appBar="clr-namespace:RemoteEducationApplication.Views.UserControls"
        Style="{StaticResource MainWindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        
        <!--menu bar-->
        <Grid Background="{StaticResource LightBlackBrush}">
            
            <!--menu-->
            <Grid Width="400"
                  HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid>
                    <TextBlock Text="File"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource BetterWhiteBrush}" />
                                <Setter Property="FontSize" Value="{StaticResource LargeFont}" />
                                <Style.Triggers>
                                    <Trigger Property ="IsMouseOver" Value="True">
                                        <Setter Property= "Foreground" Value="Red"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <TextBlock Grid.Column="1"
                           Text="View" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           FontSize="{StaticResource LargeFont}"
                           Foreground="{StaticResource BetterWhiteBrush}"/>

                <TextBlock Grid.Column="2"
                           Text="Options" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           FontSize="{StaticResource LargeFont}"
                           Foreground="{StaticResource BetterWhiteBrush}"/>

                <TextBlock Grid.Column="3"
                           Text="Help" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           FontSize="{StaticResource LargeFont}"
                           Foreground="{StaticResource BetterWhiteBrush}"/>

            </Grid>
            
            <!--minimize and close-->
            <appBar:ApplicationBar />
            
        </Grid>
        
        <!--status bar-->
        <Grid Grid.Row="2" 
              Background="{StaticResource LightBlackBrush}">

        </Grid>

    </Grid>
</Window>
